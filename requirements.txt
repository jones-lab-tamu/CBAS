# ==================================
# CBAS v3 requirements.txt
# ==================================
# This file lists the pinned dependencies for a working environment
# with Python 3.12.
#
# NOTE: torch, torchvision, and torchaudio are NOT included here.
# They are installed separately via the command in Installation.md,
# which uses the PyTorch build for CUDA 12.1. A modern NVIDIA
# driver is required for GPU support.

# Created on: 2025-07-29
# ==================================

# --- Core Backend & Application ---
Eel==0.18.2
bottle==0.13.4
bottle-websocket==0.2.9
gevent==25.5.1
gevent-websocket==0.10.1
PyYAML==6.0.2

# --- Machine Learning & Data Processing ---
transformers==4.53.3
numpy==2.2.6
pandas==2.3.1
scikit-learn==1.7.1
h5py==3.14.0
decord==0.6.0
safetensors==0.5.3
tokenizers==0.21.2
huggingface-hub==0.34.1
scipy==1.16.0

# --- GUI & Visualization ---
opencv-python==4.12.0.88
matplotlib==3.10.3
cmap==0.6.2
pillow==11.3.0

# --- Utilities ---
watchdog==6.0.0
tqdm==4.67.1

# --- Transitive Dependencies (Pinned for stability) ---
# These are dependencies of the libraries above, but we pin them
# here to guarantee the environment is identical.
certifi==2025.7.14
cffi==1.17.1
charset-normalizer==3.4.2
colorama==0.4.6
contourpy==1.3.2
cycler==0.12.1
filelock==3.18.0
fonttools==4.59.0
fsspec==2025.7.0
future==1.0.0
greenlet==3.2.3
idna==3.10
importlib_resources==6.5.2
Jinja2==3.1.4
joblib==1.5.1
kiwisolver==1.4.8
MarkupSafe==2.1.5
mpmath==1.3.0
networkx==3.3
packaging==25.0
pycparser==2.22
pyparsing==3.2.3
python-dateutil==2.9.0.post0
pytz==2025.2
regex==2024.11.6
requests==2.32.4
setuptools==80.9.0
six==1.17.0
sympy==1.13.1
threadpoolctl==3.6.0
typing_extensions==4.14.1
tzdata==2025.2
urllib3==2.5.0
zope.event==5.1.1
zope.interface==7.2

# ==================================
# Updating Dependencies
# ==================================
# To regenerate this file after upgrading packages:
#   pip freeze > requirements.txt
# Consider reviewing and curating it before committing.